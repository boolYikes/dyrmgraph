version: 2

models:
  - name: demo_vendor_summary
    columns:
      - name: vendor_id
        tests:
          - not_null
          - unique
      - name: trip_count
        tests:
          - not_null
