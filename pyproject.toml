[project]
name = "dyrmgraph-libs"
version = "0.1.0"
dependencies = ["aiohttp", "aiofiles"]

[project.optional-dependencies]
test = ["pytest", "pytest-asyncio", "mock"]

[tool.setuptools.packages.find]
include = ["libs", "libs.*"]

[tool.ruff]
line-length = 120